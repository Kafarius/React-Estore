import { useDispatch } from 'react-redux';
import { cartActions } from '../store/cart-slice';
import classes from '../css/OpinionItem.module.css';


const OpinionItem = (props) => {
    return (
        <div className={classes.opinion}>

        </div>
    );
};

export default OpinionItem;